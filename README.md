# color_pos
This Repo is about my opencv project which includes the object of specific color and opencv draw the bounding box along that object, the frame is divided into three parts and returns the position of an object in each frame of the video real time
